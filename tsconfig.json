{
  "//": "Each package owns its own standalone tsconfig (noEmit typecheck) extending tsconfig.base.json. No TS project references at the root -- packages resolve workspace cross-deps through bun/moon, not composite references.",
  "files": [],
  "include": []
}
